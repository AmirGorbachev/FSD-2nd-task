mixin rate-button(rating)
  .rate-button
    - for (var x = 0; x < rating; x++)
      .rate-button__star--active
    - for (var x = rating; x < 5; x++)
      .rate-button__star
//- Вызываем как +rate-button(rating), где rating = нужное число звёзд от 0 до 5